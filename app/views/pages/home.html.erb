<div class="container">
  <h1>Landing Page</h1>
  <!-- er + tab -->
  <!-- this WONT display in the browser -->
  <%# Time.now %>
  <!-- pe + tab -->
  <!-- this will display in the browser -->
  <%#=  %>
  <!-- a tag: lt + tab -->
  <!-- link_to takes to arguments: text && where -->
  <%#= link_to 'Text', prefix_path, options  %>
  <p>
    <%= link_to 'Contact', contact_path, class: 'btn btn-primary' %>
  </p>
  <p>
    <%= link_to 'About', about_path, class: 'btn btn-primary' %>
  </p>
</div>
